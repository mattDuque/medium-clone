import React from "react"
import Header from "../../components/Header"

function Post() {
  return (
    <main>
      <Header />
    </main>
  )
}

export default Post

export const getStaticProps = async () => {
  const query = `
    
    `
}
